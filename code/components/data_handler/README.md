# Data Handler Module

### Usage:
- Init the data handler on application start:
  - data_handler = DataHandler() 
  - This will automatically grab the food and user data and store it in the structure.
